# Guías rápidas del repositorio

## Despliegue en Plesk (Laravel)
- Abre: `docs/deploy-hosting-paso-a-paso.md`
- Incluye configuración de **Plesk** (Hosting Settings, Document Root, File Manager/SSH).
- Incluye solución para `ViteManifestNotFoundException` (`public/build/manifest.json`).
- Incluye validación rápida para cuando el Document Root ya está en `.../public`.
- Incluye cómo correr migraciones y seeders, y crear usuario admin inicial.
- Incluye cómo redirigir la ruta raíz (`/`) a `/login`.
- Incluye corrección: vendedor y admin ven todos los reportes (mismo alcance).
